import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.togapps.gods',
  appName: 'gods',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
